/// @description set height and width

var viewport_values = room_get_viewport(rm_level0, 0);
base_width = viewport_values[3]; // viewport width
base_height = viewport_values[4]; // viewport height
width = base_width;
height = base_height;